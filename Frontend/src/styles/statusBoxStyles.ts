import { StyleSheet } from "react-native";

export default StyleSheet.create({
    box: {
        height: 180,
        marginHorizontal: 20,
        marginTop: -30,
        backgroundColor: "#EBF0D9",
        borderRadius: 24,
    },
});
