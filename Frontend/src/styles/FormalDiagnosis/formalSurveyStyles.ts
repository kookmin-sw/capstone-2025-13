// styles/formalSurveyStyles.ts
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#FFFFF8",
    },
    scroll: {
        paddingBottom: 40,
    },
});

export default styles;
